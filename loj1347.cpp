using namespace std;

#include<iostream>
#include<cstring>
#include<cstdio>
#include<algorithm>

int main()
{
        int t;
        scanf("%d",&t);

        while(t--)
        {
                scanf("%s%s%s",str1,str2,str3);
                l1 = strlen(str1);
                l2 = strlen(str2);
                l3 = strlen(str3);

                l = min(l1,min(l2,l3));

                for(i=0;i)
        }
}
